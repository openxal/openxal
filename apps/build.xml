<?xml version="1.0" encoding="UTF-8"?>

<!-- Process all the applications. -->
<project name="apps" basedir="." default="all">
    <import file="../common.xml"/>		

    <!-- Build all the applications. -->
    <target name="all">
        <subant target="all">
            <property name="batch.app.build" value="true" />
            <fileset dir="." includes="*/build.xml" />
        </subant>
    </target>
    
    <!-- Clean all the applications. -->
    <target name="clean">
        <subant target="clean">
            <fileset dir="." includes="*/build.xml" />
        </subant>
        
        <delete dir="${buildroot}/compile/apps"/>
        <delete dir="${buildroot}/jar/apps"/>
    </target>
    
</project>
